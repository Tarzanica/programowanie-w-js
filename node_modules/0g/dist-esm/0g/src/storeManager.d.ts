import { Store, StoreInstance } from './stores';
export declare class StoreManager {
    private poolMap;
    acquire<S extends Store>(StoreSpec: S): StoreInstance;
    release(store: StoreInstance): void;
}
