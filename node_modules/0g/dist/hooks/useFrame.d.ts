import { Entity } from '../entity';
import { Query, QueryDef } from '../queries';
/**
 * Runs a callback every game step
 */
export declare function useFrame(callback: () => void): void;
/**
 * Iterates over a query result entity list every
 * game step
 */
export declare function useQueryFrame(input: Query<QueryDef>, callback: (entity: Entity) => void): void;
