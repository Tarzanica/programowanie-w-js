"use strict";
var __read = (this && this.__read) || function (o, n) {
    var m = typeof Symbol === "function" && o[Symbol.iterator];
    if (!m) return o;
    var i = m.call(o), r, ar = [], e;
    try {
        while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
    }
    catch (error) { e = { error: error }; }
    finally {
        try {
            if (r && !r.done && (m = i["return"])) m.call(i);
        }
        finally { if (e) throw e.error; }
    }
    return ar;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.useQuery = void 0;
var react_1 = require("react");
var useGame_1 = require("./useGame");
function useQuery(queryDef) {
    var game = useGame_1.useGame();
    // stored as a static reference.
    var _a = __read(react_1.useState(function () { return game.queries.create(queryDef); }), 1), query = _a[0];
    var _b = __read(react_1.useState(Math.random()), 2), _ = _b[0], setForceUpdate = _b[1];
    react_1.useLayoutEffect(function () {
        function onEntitiesChanged() {
            setForceUpdate(Math.random());
        }
        query.events.on('entityAdded', onEntitiesChanged);
        query.events.on('entityRemoved', onEntitiesChanged);
        return function () {
            query.events.off('entityAdded', onEntitiesChanged);
            query.events.off('entityRemoved', onEntitiesChanged);
        };
    }, [query]);
    return query;
}
exports.useQuery = useQuery;
//# sourceMappingURL=useQuery.js.map