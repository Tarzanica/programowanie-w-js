import { Entity } from '../entity';
import { Query, QueryDef } from '../queries';
import { Store } from '../stores';
export declare function useWatch(input: Query<QueryDef>, stores: Store[], callback: (entity: Entity) => void): void;
export declare function useWatch(input: Entity, stores: Store[], callback: () => void): void;
